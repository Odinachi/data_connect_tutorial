specVersion: "v1beta"
serviceId: "connectnote"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "connectnote-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
